apiVersion: v1
kind: ConfigMap
metadata:
  name: watch-config
  namespace: default
data:
  watch-config.yaml: |
    resources:
      - group: ""
        kind: Pod
      - group: "apps"
        kind: Deployment
