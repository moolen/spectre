apiVersion: v1
kind: Namespace
metadata:
  name: rca-test-direct
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: direct-deployment
  namespace: rca-test-direct
spec:
  replicas: 1
  selector:
    matchLabels:
      app: direct-test
  template:
    metadata:
      labels:
        app: direct-test
    spec:
      containers:
      - name: nginx
        image: nginx:1.25.3
        ports:
        - containerPort: 80
