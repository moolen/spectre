apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "spectre.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "spectre.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  {{- with .Values.updateStrategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- if .Values.minReadySeconds }}
  minReadySeconds: {{ .Values.minReadySeconds }}
  {{- end }}
  {{- if .Values.revisionHistoryLimit }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "spectre.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "spectre.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "spectre.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.priorityClassName }}
      priorityClassName: {{ .Values.priorityClassName }}
      {{- end }}
      {{- if .Values.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- end }}
      {{- if .Values.dnsPolicy }}
      dnsPolicy: {{ .Values.dnsPolicy }}
      {{- end }}
      {{- with .Values.dnsConfig }}
      dnsConfig:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.hostAliases }}
      hostAliases:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.hostNetwork }}
      hostNetwork: {{ .Values.hostNetwork }}
      {{- end }}
      {{- if .Values.hostPID }}
      hostPID: {{ .Values.hostPID }}
      {{- end }}
      {{- if .Values.hostIPC }}
      hostIPC: {{ .Values.hostIPC }}
      {{- end }}
      containers:
      - name: spectre
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        {{- with .Values.securityContext }}
        securityContext:
          {{- toYaml . | nindent 12 }}
        {{- end }}
        ports:
        - name: http
          containerPort: {{ .Values.service.targetPort }}
          protocol: TCP
        {{- if .Values.pprof.enabled }}
        - name: pprof
          containerPort: {{ .Values.pprof.port }}
          protocol: TCP
        {{- end }}
        args:
        - --data-dir={{ .Values.persistence.mountPath }}
        - --api-port={{ .Values.service.targetPort }}
        - --log-level={{ .Values.config.logLevel }}
        - --watcher-config=/etc/watcher/watcher.yaml
        - --segment-size={{ .Values.config.segmentSize | int64 }}
        - --max-concurrent-requests={{ .Values.config.maxConcurrentRequests }}
        {{- if .Values.pprof.enabled }}
        - --pprof-enabled=true
        - --pprof-port={{ .Values.pprof.port }}
        {{- end }}
        {{- if .Values.tracing.enabled }}
        - --tracing-enabled=true
        - --tracing-endpoint={{ .Values.tracing.endpoint }}
        {{- end }}
        {{- if .Values.graph.enabled }}
        - --graph-enabled=true
        - --graph-host=127.0.0.1
        - --graph-port={{ .Values.graph.falkordb.port }}
        - --graph-name={{ .Values.graph.falkordb.graphName }}
        - --graph-retention-hours={{ .Values.graph.sync.retentionHours }}
        - --graph-rebuild-on-start={{ .Values.graph.sync.rebuildOnStart }}
        - --graph-rebuild-if-empty={{ .Values.graph.sync.rebuildIfEmptyOnly }}
        - --graph-rebuild-window-hours={{ .Values.graph.sync.rebuildWindowHours }}
        {{- if .Values.graph.timeline }}
        - --timeline-query-source={{ .Values.graph.timeline.querySource }}
        - --timeline-mode={{ .Values.graph.timeline.mode }}
        {{- end }}
        {{- end }}
        {{- range .Values.extraArgs }}
        - {{ . }}
        {{- end }}
        volumeMounts:
        {{- if .Values.persistence.enabled }}
        - name: data
          mountPath: {{ .Values.persistence.mountPath }}
        {{- end }}
        - name: watcher-config
          mountPath: /etc/watcher
          readOnly: true
        {{- with .Values.extraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.env }}
        env:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.envFrom }}
        envFrom:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- if .Values.livenessProbe.enabled }}
        livenessProbe:
          {{- omit .Values.livenessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.readinessProbe.enabled }}
        readinessProbe:
          {{- omit .Values.readinessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.startupProbe.enabled }}
        startupProbe:
          {{- omit .Values.startupProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- with .Values.lifecycle }}
        lifecycle:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        resources:
          {{- toYaml .Values.resources | nindent 12 }}
      {{- if .Values.mcp.enabled }}
      - name: mcp
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        {{- with .Values.mcp.securityContext }}
        securityContext:
          {{- toYaml . | nindent 12 }}
        {{- end }}
        ports:
        - name: mcp
          containerPort: {{ .Values.mcp.port }}
          protocol: TCP
        command:
        - /app/spectre
        - mcp
        - --log-level=debug
        env:
        - name: SPECTRE_URL
          value: {{ .Values.mcp.spectreURL | quote }}
        - name: MCP_HTTP_ADDR
          value: {{ .Values.mcp.httpAddr | quote }}
        {{- if .Values.graph.enabled }}
        - name: GRAPH_ENABLED
          value: "true"
        - name: GRAPH_HOST
          value: "localhost"
        - name: GRAPH_PORT
          value: {{ .Values.graph.falkordb.port | quote }}
        - name: GRAPH_NAME
          value: {{ .Values.graph.falkordb.graphName | quote }}
        {{- end }}
        {{- if .Values.mcp.livenessProbe.enabled }}
        livenessProbe:
          {{- omit .Values.mcp.livenessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.mcp.readinessProbe.enabled }}
        readinessProbe:
          {{- omit .Values.mcp.readinessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        resources:
          {{- toYaml .Values.mcp.resources | nindent 12 }}
      {{- end }}
      {{- if and .Values.graph.enabled .Values.graph.falkordb.sidecar }}
      - name: falkordb
        image: "{{ .Values.graph.falkordb.image.repository }}:{{ .Values.graph.falkordb.image.tag }}"
        imagePullPolicy: {{ .Values.graph.falkordb.image.pullPolicy }}
        {{- with .Values.graph.falkordb.securityContext }}
        securityContext:
          {{- toYaml . | nindent 12 }}
        {{- end }}
        ports:
        - name: redis
          containerPort: {{ .Values.graph.falkordb.port }}
          protocol: TCP
        - name: browser
          containerPort: 3000
          protocol: TCP
        volumeMounts:
        {{- if .Values.graph.falkordb.persistence.enabled }}
        - name: graph-data
          mountPath: {{ .Values.graph.falkordb.persistence.mountPath }}
        {{- end }}
        {{- if .Values.graph.falkordb.livenessProbe.enabled }}
        livenessProbe:
          {{- omit .Values.graph.falkordb.livenessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        {{- if .Values.graph.falkordb.readinessProbe.enabled }}
        readinessProbe:
          {{- omit .Values.graph.falkordb.readinessProbe "enabled" | toYaml | nindent 10 }}
        {{- end }}
        resources:
          {{- toYaml .Values.graph.falkordb.resources | nindent 12 }}
      {{- end }}
      volumes:
        {{- if .Values.persistence.enabled }}
      - name: data
        persistentVolumeClaim:
          claimName: {{ default (include "spectre.fullname" .) .Values.persistence.existingClaim }}
        {{- end }}
      - name: watcher-config
        configMap:
          name: {{ include "spectre.fullname" . }}
      {{- if and .Values.graph.enabled .Values.graph.falkordb.sidecar .Values.graph.falkordb.persistence.enabled }}
      - name: graph-data
        persistentVolumeClaim:
          claimName: {{ include "spectre.fullname" . }}-graph
      {{- end }}
      {{- with .Values.extraVolumes }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
